{
  "name": "holonext",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev:cart": "cd examples/cart && bun run dev",
    "dev:agent": "cd examples/agent-client && bun run dev",
    "typecheck": "bun --filter '*' run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "latest"
  }
}
